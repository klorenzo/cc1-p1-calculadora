; Constantes utilizadas en todo el programa.

(define SUMA "+")
(define RESTA "-")
(define DIVISION "/")
(define MULTIPLICACION "*")
(define RESIDUO "%")
(define COCIENTE "div")

(define PI 3.141592654)

(define SQUARE_ROOT "sqroot")
(define SQUARE_SIMPLE "sqr")
(define SENO "sen")
(define COSENO "cos")
(define TANGENTE "tan")
(define FACTORIAL "fact!")

(define RESPUESTA "respuesta   >> ")
(define NOT_VALID_EXPRESSSION "ERROR! Expresión No Válida.")
(define DIVISION_BY_ZERO "ERROR! Division by Zero.")
(define NEGATIVE_SQUARE_ROOT "ERROR! Negative Square Root.")
(define IMPOSSIBLE_FACTORIAL "ERROR! Impossible to calculate the factorial.")
